# .env.example - copy to .env and fill values

# Server
PORT=5005
CLIENT_URL=http://localhost:5173

# MongoDB (use MongoDB Atlas connection string or local mongodb)
MONGO_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/myFirstDatabase?retryWrites=true&w=majority

# Auth
JWT_SECRET=replace_with_a_strong_random_string

# Optional: set NODE_ENV=production in production environments
